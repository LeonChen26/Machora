{
  "name": "@machora/standalone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/start.js",
  "scripts": {
    "dev": "tsx src/start.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "cross-env NODE_ENV=production node dist/start.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@machora/shared": "workspace:*",
    "@machora/worker": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "cross-env": "^10.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0"
  }
}
